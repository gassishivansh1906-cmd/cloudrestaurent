# Optional. By default the app calls "/api" (relative), which works behind Nginx.
# Set this only if you want to point the dev frontend at a different API base URL.
# VITE_API_URL=http://localhost:5000/api

# Used by the Vite dev proxy (npm run dev) to reach the backend.
# VITE_API_PROXY=http://localhost:5000
